export const metadata = {
  title: "VoC Intel | Call Convert",
  description: "Voice of Customer Copy Intelligence Engine",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
